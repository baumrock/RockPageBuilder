<?php namespace RMBlock;

use ProcessWire\Inputfield;
use ProcessWire\RockFieldsField;
use RockMatrix\Block;
use RockMatrix\BlockSettingsArray;

class {name} extends Block {

  const prefix = "rmblock_{namelower}_";

  public function info() {
    return [
      'title' => '{name}',
      'color' => 'lime',
      // 'icon' => 'picture-o',
      // 'description' => 'RockMatrix Block Setup Demo',
      // 'sort' => 500,
    ];
  }

  /** ##### magic methods ##### */

  // /**
  //  * buildForm can be used to modify the GUI both for the matrix editor as well
  //  * as when the block is edited in a new window
  //  */
  // public function buildForm($form) {
  //   // here you can edit the block's inputfields at runtime
  //   if($f = $form->get('title')) {
  //     $f->label = 'Title Label Changed';
  //     $f->notes = 'Title Note';
  //   }
  // }

  // /**
  //  * buildFormMatrix modifies the GUI only when rendered as a single block
  //  * inside a regular page editor matrix field
  //  */
  // public function buildFormMatrix($form) {
  //   // here you can edit the block's inputfields at runtime
  //   if($f = $form->get('title')) {
  //     $f->label = 'Title Label Changed';
  //     $f->notes = 'Title Note';
  //   }
  // }

  public function onCreate() {
    // we dont execute resetting the title if the block is being cloned
    if($this->matrix()->isClone) return;
    // set default field values when a new block is created
    $this->setInAllLanguages('title', '');
  }

  /** ##### frontend ##### */

  /** ##### backend ##### */

  // /**
  //  * Return GUI label of this block
  //  */
  // public function getLabel() {
  //   return $this->title;
  // }

  public function migrate() {
    parent::migrate();
    $rm = $this->rm();

    // if you need to remove a field that you previously added you can do this:
    // $rm->removeField('your_old_field', true);

    $rm->migrate([
      'fields' => [
        // here you can create new fields if needed
        // you can also create fields via the GUI
      ],
      'templates' => [
        $this->getTplName() => [
          // here you set the fields of the block-template
          // if you need to remove a field see the removeField() demo above
          // alternatively you can use the "fields-" key to remove all fields
          // that are not listed in the array. note that this will remove
          // fields that were added via GUI!
          'fields' => [
            'title' => [
              'required' => false,
              // 'collapsed' => Inputfield::collapsedHidden,
            ],
          ],
        ],
      ],
    ]);
  }

  /**
   * Define settings for this block
   * You can access settings in your view file like this:
   * $block->settings('demo_setting', 'optional-default-value')
   */
  // public function settingsTable(RockFieldsField $field) {
  //   /** @var BlockSettingsArray $settings */
  //   $settings = $this->matrix()->cloneBlockSettings($field);
  //   $settings->add([
  //     'name' => 'demo_setting',
  //     'label' => 'Your Setting Label',
  //     'label123' => 'Label in other language',
  //     'value' => $field->input('demo_setting', 'select', [
  //       '*s' => 'small',
  //       'm' => 'medium',
  //       'l' => 'large',
  //     ]),
  //   ]);
  //   return $settings;
  // }

}
